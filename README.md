# dev-log
An enterprise-grade, lightweight hybrid log investigator and root cause analysis (RCA) utility. It dynamically maps asynchronous transaction lifecycles across microservices from a single ID. It merges legacy Oracle/MSSQL logs with Graylog and RabbitMQ streams into a visual pipeline, automatically generating actionable RCA action plans.
